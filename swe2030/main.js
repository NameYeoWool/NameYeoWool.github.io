var URLS = {
    prefix:
    list:
    eachList:
    groupAdd:
};

data = {
    data:[{},{},{}]
}
function network(command, end, arg){
    switch(commnad){

    case"addList":
    $.get(URLS.)



    // data.data.push(name)
    // end

    case"list":

    //아래와 같이 end 만 넣어 주어도 서버와 통신하는 것 같이 돌아간다.
    //#.get(URLS.prefix + URLS.list, end);
    end{data[...]}
    }
}